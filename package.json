{
  "name": "mongoose-resource",
  "version": "1.0.0",
  "description": "Configure schemas and resource handlers on mongoose the same way models are configured.",
  "main": "mongoose-resource.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tim Lind <tim@incremental.co>",
  "license": "ISC"
}
